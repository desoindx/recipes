import styled from 'styled-components';

export const Container = styled.div`
  overflow: scroll;
  max-height: 100vh;
  padding: 64px;
  display: flex;
  flex-wrap: wrap;
  justify-content: space-evenly;
  gap: 32px;
`;

